{% extends 'base.html.twig' %}

{% block title %}Banks{% endblock %}

{% block body %}
    <a href="{{ url('bank.create') }}" class="btn btn-dark ml-auto">Create New</a>
    <div class="container mt-3 mb-4">
        <table class="table table-hover table-sm">
            <tr class="thead-dark">
                <th>Name</th>
                <th>Created At</th>
                <th style="text-align: center;">Actions</th>
            </tr>
            {% for bank in banks %}
                <tr>
                    <td>
                        <a href="{{ url('bank.view', {id: bank.id}) }}">{{ bank.name }}</a>
                    </td>
                    <td>{{ bank.createdAt|date("d/m/Y h:s") }}</td>
                    <td style="text-align: center;">
                        <div>
                            <a href="{{ url('bank.edit', {id: bank.id}) }}" style="margin: 5px; text-decoration: none;">
                                <button type="button" class="btn btn-secondary" title="Edit">
                                    <span class="octicon octicon-pencil"></span>
                                </button>
                            </a>
                            <form action="{{ url('bank.delete', {id: bank.id}) }}" method="post" style="margin: 5px; display: inline;">
                                <button type="submit" class="btn btn-danger" title="Delete" onclick="return confirm('Are you sure you want to delete this bank?');">
                                    <span class="octicon octicon-trashcan"></span>
                                </button>
                            </form>
                        </div>
                    </td>
                </tr>
            {% endfor %}
        </table>
    </div>
{% endblock %}
